def detect_changes(image_before, image_after, index_type):
    """
    Perform before/after change detection for a specific index (e.g., NDVI, NDWI).
    Returns the difference image or statistics.
    """
    pass
